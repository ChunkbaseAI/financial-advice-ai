[project]
name = "financial-advice-ai"
version = "0.1.0"
description = "Python mirror of the claim-card case-set schema and checks."
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.4"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra"
