# Copy to .env and fill in. Never commit the real .env.
# Vercel AI Gateway key: serves Jev (typesafe-ai/jev) and every LLM comparator.
AI_GATEWAY_API_KEY=

# Optional fallback only, if the gateway route becomes unavailable.
# AWS_BEDROCK_ACCESS_KEY_ID=
# AWS_BEDROCK_SECRET_ACCESS_KEY=
# AWS_BEDROCK_REGION=
